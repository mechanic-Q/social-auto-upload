# Social Auto Upload

将同一条视频（或图文）自动发布到多个社媒平台的命令行工具，并对已发布作品做跨平台运营数据采集。每个平台是一个独立的发布通道，共享统一的账号（Cookie）管理与元数据（标题、简介、话题标签、封面、定时）抽象。

## Language

### 元数据

**话题标签 (Tag)**:
以 `#` 引导、必须命中平台已有话题库的关键词，用于把作品归入平台话题流量池。一个作品可带多个，数量受平台实际限制。
_Avoid_: hashtag（代码内部命名可用 `tags`）、关键词、话题（单独说"话题"时指平台话题库中的词条）

**固定标签清单 (Extra Tags)**:
发布时自动追加在话题标签之后的一组预定义高流量标签（如"上热门"），内容不随作品变化。存于本地配置，各平台可各自维护。
_Avoid_: 热门标签（歧义，可能被理解为"平台热榜话题"）、默认标签

**封面 (Thumbnail)**:
发布时手动指定的首图，与平台自动截取的视频帧相对。一个平台可能接受多种比例（横版/竖版/方形），比例是封面的属性而非封面的种类。
_Avoid_: cover（代码内部命名可用 `thumbnail`）、首图、海报

### 活动与热点

**创作活动 (Activity)**:
平台官方发起、需在发布页显式选择参加、可换取流量扶持的运营活动（如快手的"活动信息"、头条的"活动广场"条目）。活动独立于话题标签存在：参加活动不等于打话题标签。
_Avoid_: 挑战、征文（均为活动的具体形式）、热点

**活动发现 (Activity Discovery)**:
在发布之外、独立列出平台当前可参加的创作活动的过程。发现的结果是活动的候选清单，供后续发布时选用。
_Avoid_: 热搜、榜单（指内容热度排行，与创作活动无关）

### 发布过程

**发布通道 (Uploader)**:
针对单一平台的发布实现，包含登录、Cookie 校验与视频/图文发布。平台与通道一一对应。
_Avoid_: adapter、driver

**账号 (Account)**:
某一发布通道下经扫码登录并获得的一组凭据（Cookie），一个通道可存多个账号。账号名是用户起的别名，不是平台用户名。
_Avoid_: 用户、profile

**定时发布 (Schedule)**:
指定作品在未来的某个时间点由平台侧自动可见。区别于本工具立即提交发布的行为。
_Avoid_: 预约、延迟发布

**草稿箱验收 (Draft Verification)**:
让发布流程走完所有交互（上传、填字段、选封面与活动）但止步于存入平台草稿箱、不公开作品的验证方式。用于安全地验收发布通道。
_Avoid_: 干跑（未定义到哪一步为止）、静默发布

### 数据采集

**采集器 (Collector)**:
针对单一平台、读取创作者后台中本人账号已发布作品数据的实现。平台与采集器一一对应，与发布通道共用账号凭据。
_Avoid_: 爬虫（口语可用，术语统一为采集器）、data center

**作品 (Work)**:
已发布到平台的内容单元（视频或图文），是数据采集与分析的对象。本地素材经发布成功后才对应到作品。
_Avoid_: 视频（图文也是作品）、笔记（小红书方言）、内容（过泛）

**快照 (Snapshot)**:
某一采集时刻对作品或账号指标取值的只追加记录；指标历史即快照序列，旧值永不覆盖。
_Avoid_: 更新（暗示覆盖）、统计（指聚合结果）

**全量采集 (Full Collection)**:
对账号下作品整体进行的一次常规采集，是各作品指标曲线逐日连续的保障。
_Avoid_: 定时任务（指调度机制而非采集本身）、巡检

**增量采集 (Incremental Collection)**:
发布成功后延迟触发的、只针对本次发布平台新作品的一次采集，用于锁定首日数据起点。
_Avoid_: 补采（指对缺失数据的回补）、即时采集
